class HCSTagline {
    constructor(tagline) {
        this.tagline = tagline;
    }

    // setter
    set setTagline(tagline) {
        this.tagline = tagline;
    }

    // getter
    get getTagline() {
        return this.tagline;
    }

}

export default HCSTagline